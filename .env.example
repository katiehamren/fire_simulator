# Copy to .env and fill in (never commit .env).
# Used by the chatbot (OpenAI function-calling, optional web search).
# python-dotenv loads retirement_simulator/.env when you run the app.

OPENAI_API_KEY=
